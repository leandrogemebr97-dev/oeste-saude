[phases.setup]
nixPkgs = ["...", "python312"]
nixLibs = ["...", "zlib"]

[phases.build]
cmds = ["python -m pip install -r requirements.txt", "python -m playwright install chromium"]

[start]
cmd = "gunicorn main:app --host 0.0.0.0 --port $PORT --workers 4"

[variables]
PORT = "8001"
